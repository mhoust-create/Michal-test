name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      # Checkout main to workspace root (keeps .git here for deploy action)
      - uses: actions/checkout@v4

      # Checkout supplements branch to a subdirectory
      - uses: actions/checkout@v4
        with:
          ref: claude/vitamins-supplements-app-br0yw2
          path: supplements-app

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      # Build behaviour tracker (workspace root)
      - run: npm install --legacy-peer-deps
      - run: npm run build

      # Build supplements tracker
      - run: npm install --legacy-peer-deps
        working-directory: supplements-app
      - run: npm run build
        working-directory: supplements-app

      # Nest supplements build inside behaviour dist
      - run: mkdir -p dist/supplements && cp -r supplements-app/dist/. dist/supplements/

      # Deploy dist/ to gh-pages branch
      - uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages
          clean: true
